# 🤖 Кулинарный голосовой бот для Telegram

Бот, который распознает голосовые сообщения с продуктами и предлагает рецепты блюд.

## 📋 Возможности

- 🎤 Распознавание голосовых сообщений на русском языке
- 🍳 Генерация рецептов на основе доступных продуктов
- 💡 Умное определение намерений (выбор блюда или добавление продуктов)
- 📝 Хранение контекста диалога

## 🚀 Установка

### 1. Системные зависимости

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install python3-pyaudio ffmpeg
```

**macOS:**
```bash
brew install portaudio ffmpeg
```

**Windows:**
- Скачайте и установите [FFmpeg](https://ffmpeg.org/download.html)
- Добавьте FFmpeg в PATH

### 2. Python зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройка API ключей

Создайте файл `.env` или экспортируйте переменные окружения:

```bash
export TELEGRAM_TOKEN="your_telegram_bot_token"
export GROQ_API_KEY="your_groq_api_key"
export UNSPLASH_ACCESS_KEY="your_unsplash_key"  # Опционально
```

**Получение ключей:**

- **Telegram Bot Token**: [@BotFather](https://t.me/botfather) → /newbot
- **Groq API Key**: [console.groq.com](https://console.groq.com/)
- **Unsplash Key** (опционально): [unsplash.com/developers](https://unsplash.com/developers)

### 4. Запуск

```bash
python main.py
```

## 📱 Использование

1. Отправьте боту `/start`
2. Запишите голосовое сообщение с перечислением продуктов
3. Получите список блюд
4. Назовите голосом желаемое блюдо или добавьте продукты
5. Получите детальный рецепт с фото

## 🏗️ Структура проекта

```
.
├── main.py              # Точка входа
├── config.py            # Конфигурация и настройки
├── handlers.py          # Обработчики команд и сообщений
├── utils.py             # Распознавание речи
├── groq_service.py      # Работа с Groq API
├── image_service.py     # Поиск изображений
├── state_manager.py     # Управление состоянием
├── requirements.txt     # Зависимости
└── temp/               # Временные файлы (создается автоматически)
```

## ⚙️ Настройки

В `config.py` можно изменить:

- `GROQ_MODEL` - модель Groq (по умолчанию: llama-3.1-70b-versatile)
- `SPEECH_LANGUAGE` - язык распознавания (по умолчанию: ru-RU)
- `MAX_HISTORY_MESSAGES` - размер истории диалога

## 🐛 Устранение неполадок

**Ошибка PyAudio:**
```bash
# Ubuntu/Debian
sudo apt-get install portaudio19-dev python3-pyaudio

# macOS
brew install portaudio
pip install pyaudio
```

**Ошибка FFmpeg:**
- Убедитесь, что FFmpeg установлен и доступен в PATH
- Проверьте: `ffmpeg -version`

**Ошибка распознавания речи:**
- Проверьте качество голосового сообщения
- Говорите четко и не слишком быстро
- Проверьте интернет-соединение (используется Google Speech Recognition)

## 📝 Лицензия

MIT License
